define(function(){

	var requireConfig = {
		baseUrl : "$jive/src/",
		paths : {
			postal 	: "../libs/postal",
			todo 	: "../libs/todo",
			lodash 	: "../vendor/lodash"
		}
	};

	return requireConfig;

});