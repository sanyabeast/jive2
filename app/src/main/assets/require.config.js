define(function(){

	var requireConfig = {
		baseUrl : "$jive/src/",
		paths : {
			postal 	: "../libs/postal",
			todo 	: "../libs/todo",
			jsterm 	: "../libs/jsterm",
			lodash 	: "../vendor/lodash"
		}
	};

	return requireConfig;

});